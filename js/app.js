const App = React.createClass({
    render: function () {
        return (<div>
            <button>prevew</button>
        </div>)
    }
});
ReactDOM.render(<App/>, document.getElementById('content'));
